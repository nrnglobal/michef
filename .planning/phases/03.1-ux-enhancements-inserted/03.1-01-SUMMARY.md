---
phase: 03.1-ux-enhancements-inserted
plan: 01
subsystem: ui
tags: [inventory, supabase, lucide-react, react, nextjs, shopping-list]

# Dependency graph
requires:
  - phase: 03.1-ux-enhancements-inserted
    provides: Phase context and decisions D-01 through D-12 for inventory enhancements
provides:
  - is_staple boolean column migration for fridge_staples table (006_add_is_staple.sql)
  - Updated FridgeStaple type with is_staple optional field
  - Inventory page with 4-tab category bar (Show All / Fridge / Pantry / Spices)
  - Star-toggle per item row persisting is_staple to Supabase
  - Staples filter button narrowing view to is_staple=true items
  - Add-to-shopping-list button per item row inserting into next confirmed plan's list
affects: [03.1-02-shopping-list, shopping-list-items, fridge_staples]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Star/ShoppingCart lucide-react icons used for staple toggle and add-to-list actions"
    - "Tab state managed as 'all' | 'fridge' | 'pantry' | 'spices' union type in client component"
    - "fridgeCats array groups Produce/Protein/Dairy/Grains/Other into Fridge tab"
    - "Error/success messages use var(--casa-diff-del-text/bg) and var(--casa-primary/primary-bg) CSS vars"

key-files:
  created:
    - supabase/migrations/006_add_is_staple.sql
  modified:
    - lib/types.ts
    - app/(client)/inventory/page.tsx
    - app/(client)/inventory/inventory-client.tsx

key-decisions:
  - "Use var(--casa-diff-del-text) and var(--casa-diff-del-bg) for error/destructive states — no separate casa-destructive variable defined in globals.css"
  - "Server-side category grouping removed from page.tsx; all filtering and grouping now done client-side in inventory-client.tsx"
  - "supabase db push requires manual linking — migration file provided for manual execution against hosted instance"
  - "Inline success/error messages (2-second timeout) used for shopping list feedback — no external toast library"

patterns-established:
  - "Tab filter pattern: activeTab state + fridgeCats array + staplesFilter boolean applied in sequence"
  - "Shopping list add pattern: find confirmed plan → get shopping_lists by menu_plan_id → insert to shopping_list_items"

requirements-completed: [UX-01]

# Metrics
duration: 12min
completed: 2026-03-25
---

# Phase 3.1 Plan 01: Inventory Enhancements Summary

**Inventory page rebuilt with category tabs (Show All/Fridge/Pantry/Spices), star-toggle staple persistence via Supabase, staples filter, and one-tap add-to-shopping-list with inline error/success feedback**

## Performance

- **Duration:** 12 min
- **Started:** 2026-03-25T05:05:18Z
- **Completed:** 2026-03-25T05:17:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Created migration 006_add_is_staple.sql and updated FridgeStaple type in lib/types.ts
- Rebuilt inventory-client.tsx with tab bar, staple toggle, staples filter, and add-to-shopping-list functionality
- Removed server-side category grouping from page.tsx; client now handles all filtering and grouping

## Task Commits

Each task was committed atomically:

1. **Task 1: Migration + type update for is_staple column** - `7a814c6` (feat)
2. **Task 2: Rebuild inventory page with tabs, staple toggle, staples filter, and add-to-shopping-list** - `6d2b2b0` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified

- `supabase/migrations/006_add_is_staple.sql` - ALTER TABLE fridge_staples ADD COLUMN IF NOT EXISTS is_staple BOOLEAN DEFAULT false
- `lib/types.ts` - Added `is_staple?: boolean` to FridgeStaple interface
- `app/(client)/inventory/page.tsx` - Simplified to pass allItems directly to InventoryClient; removed server-side grouping
- `app/(client)/inventory/inventory-client.tsx` - Full rewrite: tabs, star toggle, staples filter, add-to-shopping-list, all category grouping now client-side

## Decisions Made

- Used `var(--casa-diff-del-text)` and `var(--casa-diff-del-bg)` for error/destructive button colors since `--casa-destructive` is not defined in globals.css; these existing red-tone variables serve the same purpose
- Moved all category grouping logic to the client component to support interactive tab filtering without server round-trips
- `supabase db push` failed due to missing project link; migration SQL is documented in the file for manual execution

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed hardcoded hex color values**
- **Found during:** Task 2 verification
- **Issue:** Delete button used `#DC2626` (from original file) and error message used `#fef2f2` as hex fallback inside `var()` — violated "no hardcoded hex" acceptance criterion
- **Fix:** Replaced both with existing `var(--casa-diff-del-text)` and `var(--casa-diff-del-bg)` CSS custom properties which are defined in globals.css for both light and dark mode
- **Files modified:** app/(client)/inventory/inventory-client.tsx
- **Verification:** `grep "#[0-9A-Fa-f]" inventory-client.tsx` returns no matches
- **Committed in:** 6d2b2b0 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - color consistency)
**Impact on plan:** Fix required for acceptance criterion compliance. No scope creep.

## Issues Encountered

- `supabase db push` could not run because the CLI is not linked to a project (`supabase link` not configured). Migration file is correct and available at `supabase/migrations/006_add_is_staple.sql` for manual execution via the Supabase dashboard SQL editor.

## User Setup Required

Run the following SQL in the Supabase dashboard SQL editor to apply the migration:

```sql
ALTER TABLE fridge_staples ADD COLUMN IF NOT EXISTS is_staple BOOLEAN DEFAULT false;
```

## Next Phase Readiness

- Inventory page is fully enhanced; ready for Plan 02 (shopping list manual add)
- The `is_staple` column migration must be applied to the database before the star toggle will persist correctly
- All CSS custom properties used are defined in both light and dark mode in globals.css

---
*Phase: 03.1-ux-enhancements-inserted*
*Completed: 2026-03-25*
