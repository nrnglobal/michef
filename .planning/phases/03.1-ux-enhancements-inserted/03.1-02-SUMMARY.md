---
phase: 03.1-ux-enhancements-inserted
plan: "02"
subsystem: ui
tags: [supabase, menu-plans, shopping-list, dark-mode, lucide-react, dialog]

# Dependency graph
requires:
  - phase: 03.1-01
    provides: inventory CRUD, fridge_staples is_staple column
provides:
  - addRecipeToMenuPlan server action with duplicate detection
  - Add to Menu modal on recipe detail page showing draft menus
  - Menu editor supports up to 10 recipes (client + server validation)
  - Date heading shows Pencil icon for clear edit affordance
  - All menu detail page colors use var(--casa-*) CSS custom properties
  - Shopping list add-item form with bilingual defaulting (pre-existing, verified)
affects: [menus, recipes, shopping-lists]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "addRecipeToMenuPlan pattern: client fetches draft menus via supabase client, calls server action, shows inline confirmation"
    - "Dark mode: all menu page colors via var(--casa-*) CSS custom properties, never hardcoded hex"

key-files:
  created: []
  modified:
    - lib/menu-actions.ts
    - app/(client)/recipes/[id]/recipe-detail-actions.tsx
    - app/(client)/menus/[id]/page.tsx
    - app/(client)/menus/[id]/recipe-picker-modal.tsx

key-decisions:
  - "addRecipeToMenuPlan returns error object (not throws) so client can show inline message for duplicate detection"
  - "confirmMenuPlan now validates: recipeIds.length > 10 throws, recipeIds.length < 2 throws (server mirrors client limits)"
  - "Pencil icon replaces 'tap date to edit' hint text — visual affordance is cleaner than text"
  - "Shopping list add form was already complete from quick task 260324-ccy; Task 3 was verification only"

patterns-established:
  - "Menu action modals: fetch data on modal open, show loading state, inline message on success/error"
  - "Dark mode colors: var(--casa-text), var(--casa-text-muted), var(--casa-text-faint), var(--casa-surface), var(--casa-border), var(--casa-primary)"

requirements-completed: [UX-02, UX-03, UX-04]

# Metrics
duration: 4min
completed: 2026-03-25
---

# Phase 3.1 Plan 02: Menu UX Enhancements Summary

**addRecipeToMenuPlan server action, Add to Menu modal on recipe detail, menu editor raised to 10 recipes with full dark-mode color audit and pencil date affordance**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-25T05:05:18Z
- **Completed:** 2026-03-25T05:09:06Z
- **Tasks:** 3 (Task 3 was verification-only — pre-existing code)
- **Files modified:** 4

## Accomplishments

- New `addRecipeToMenuPlan` server action with duplicate detection returning `{ error: 'already_on_menu' }` on collision
- "Add to Menu" button on recipe detail page opens draft-menu picker modal with date, recipe count, and inline confirmation
- Menu editor now supports up to 10 recipes — client button disabled at 10, `confirmMenuPlan` validates server-side
- Date heading displays Pencil icon always visible alongside the formatted date; "tap date to edit" hint removed
- All hardcoded hex colors in `menus/[id]/page.tsx` replaced with `var(--casa-*)` CSS custom properties (semantic rationale/warnings boxes preserved)
- Shopping list add form (with all D-14/D-15 requirements) verified complete from prior quick task

## Task Commits

1. **Task 1: Add addRecipeToMenuPlan server action and Add to Menu modal** - `76c8a50` (feat)
2. **Task 2: Fix menu editor — recipe limit, dark mode, date pencil** - `5822672` (feat)
3. **Task 3: Shopping list add form** - no commit (pre-existing, verified only)

## Files Created/Modified

- `lib/menu-actions.ts` - Added `addRecipeToMenuPlan` export; added `confirmMenuPlan` server-side count validation (max 10, min 2)
- `app/(client)/recipes/[id]/recipe-detail-actions.tsx` - Added CalendarPlus button, `openMenuModal`/`handleAddToMenu` handlers, draft-menu picker Dialog
- `app/(client)/menus/[id]/page.tsx` - maxRecipes 4→10, Pencil import, date display updated, all hex colors → var(--casa-*), empty state text updated
- `app/(client)/menus/[id]/recipe-picker-modal.tsx` - maxRecipes default 4→10

## Decisions Made

- `addRecipeToMenuPlan` returns `{ error: 'already_on_menu' }` (not throws) so the client can show a friendly inline message without an error boundary
- Server-side count guard added to `confirmMenuPlan` to match client: `> 10` throws "Maximum 10 recipes per menu plan"; `< 2` throws "Minimum 2 recipes required"
- Pencil icon replaces the "tap date to edit" hint text — cleaner visual affordance, consistent with established icon patterns
- Task 3 shopping list add form was already fully implemented by quick task 260324-ccy; only verification was needed

## Deviations from Plan

None — plan executed exactly as written. Task 3 was a verification task; all required code was already present.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None — all fields are wired to live Supabase data.

## Next Phase Readiness

- Recipe-to-menu shortcut ready for use from any recipe detail page
- Menu editor now supports up to 10 recipes with full dark mode compatibility
- Shopping list manual add is operational
- Phase 03.1 Plan 01 (inventory) required separately — this plan has no dependencies on it

---
*Phase: 03.1-ux-enhancements-inserted*
*Completed: 2026-03-25*
