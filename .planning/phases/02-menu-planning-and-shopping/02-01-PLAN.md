---
phase: 02-menu-planning-and-shopping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proxy.ts
  - lib/supabase/server.ts
  - supabase/migrations/003_rls_tighten.sql
  - components/sidebar.tsx
  - components/bottom-nav.tsx
  - lib/i18n/en.json
  - lib/i18n/es.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "proxy.ts protects /menus routes from cook role and /lista route from client role"
    - "RLS policies restrict menu_plan writes to client role and shopping_list_item INSERT to both roles, shopping_list_item UPDATE to cook role"
    - "lib/supabase/server.ts has no placeholder fallback — crashes immediately if env vars are missing"
    - "Sidebar Menu Planning href updated to /menus but kept active: false per D-01 (dashboard CTA is primary entry point)"
    - "Bottom nav shows Lista de compras pointing to /lista"
    - "i18n dictionaries contain all new Phase 2 UI strings in both en.json and es.json"
  artifacts:
    - path: "proxy.ts"
      provides: "Route protection for /menus and /lista"
      contains: "/lista"
    - path: "supabase/migrations/003_rls_tighten.sql"
      provides: "Role-scoped RLS policies"
      contains: "role = 'cook'"
    - path: "lib/supabase/server.ts"
      provides: "Server client without placeholder fallback"
    - path: "lib/i18n/en.json"
      provides: "English strings for menu planning and shopping list"
      contains: "menu.planMenu"
    - path: "lib/i18n/es.json"
      provides: "Spanish strings for cook shopping list"
      contains: "shopping.runningCount"
  key_links:
    - from: "proxy.ts"
      to: "app/(client)/menus/"
      via: "route protection check"
      pattern: "pathname.startsWith\\('/menus'\\)"
    - from: "components/sidebar.tsx"
      to: "/menus"
      via: "navItems href"
      pattern: "href: '/menus'"
---

<objective>
Fix three infrastructure tech debt items (proxy route protection, RLS policy tightening, env fallback removal) and prepare the foundation for Phase 2 UI: install shadcn components, update sidebar href and cook bottom nav, and add all i18n strings.

Purpose: Phase 2 pages cannot ship without INFRA fixes — proxy must protect new routes, RLS must scope writes by role, and the env fallback must not mask misconfiguration. Navigation and i18n must be ready before any page work begins.
Output: Infrastructure hardened, navigation updated, i18n complete, shadcn dialog/checkbox/separator installed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-menu-planning-and-shopping/02-CONTEXT.md
@.planning/phases/02-menu-planning-and-shopping/02-RESEARCH.md
@.planning/phases/02-menu-planning-and-shopping/02-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/types.ts:
```typescript
export interface Profile {
  id: string
  user_id: string
  role: 'client' | 'cook'
  name: string
  language: 'en' | 'es'
  created_at: string
  updated_at: string
}
```

From proxy.ts (current route protection — lines 88-103):
```typescript
// Cook trying to access client routes
if (role === 'cook' && pathname.startsWith('/dashboard')) {
  // redirects to /visita
}
// Client trying to access cook routes
if (role === 'client' && (pathname.startsWith('/visita') || pathname.startsWith('/recetas'))) {
  // redirects to /dashboard
}
```

From components/sidebar.tsx (navItems array — lines 20-63):
```typescript
const navItems = [
  { key: 'nav.dashboard', href: '/dashboard', icon: LayoutDashboard, active: true },
  { key: 'nav.recipes', href: '/recipes', icon: BookOpen, active: true },
  { key: 'nav.menuPlanning', href: '/menu-planning', icon: CalendarDays, active: false },
  { key: 'nav.shoppingLists', href: '/shopping-lists', icon: ShoppingCart, active: false },
  // ...
]
```

From components/bottom-nav.tsx (cookNavItems — lines 8-13):
```typescript
const cookNavItems = [
  { labelEs: 'Proxima Visita', href: '/visita', icon: Home },
  { labelEs: 'Compras', href: '/compras', icon: ShoppingCart },
  { labelEs: 'Recetas', href: '/recetas', icon: BookOpen },
  { labelEs: 'Mensajes', href: '/mensajes', icon: MessageSquare },
]
```

From lib/supabase/server.ts (line 8 — placeholder fallback):
```typescript
process.env.NEXT_PUBLIC_SUPABASE_URL ?? 'https://placeholder.supabase.co',
process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ?? 'placeholder-key',
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix infrastructure — proxy routes, RLS migration, env fallback</name>
  <files>proxy.ts, lib/supabase/server.ts, supabase/migrations/003_rls_tighten.sql</files>
  <read_first>
    - proxy.ts (current route protection logic, lines 78-103)
    - lib/supabase/server.ts (placeholder fallback on lines 8-9)
    - supabase/migrations/001_schema.sql (current RLS policies, lines 140-291)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md (confirm proxy.ts is correct name)
  </read_first>
  <action>
    **INFRA-01 — proxy.ts route protection (per D-01, no rename needed — proxy.ts IS correct for Next.js 16):**
    In proxy.ts, update the role-based route protection section (lines 78-103):

    1. Add `/menus` to the cook redirect block (cook cannot access client menu planning):
    ```typescript
    if (role === 'cook' && (pathname.startsWith('/dashboard') || pathname.startsWith('/menus') || pathname.startsWith('/recipes'))) {
    ```

    2. Add `/lista` to the client redirect block (client cannot access cook shopping list):
    ```typescript
    if (role === 'client' && (pathname.startsWith('/visita') || pathname.startsWith('/recetas') || pathname.startsWith('/lista'))) {
    ```

    Note: Also add `/recipes` to cook block — cook should not access the English recipe management UI. And keep existing protections.

    **INFRA-03 — Remove env fallback in lib/supabase/server.ts:**
    Change line 8 from:
    ```typescript
    process.env.NEXT_PUBLIC_SUPABASE_URL ?? 'https://placeholder.supabase.co',
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY ?? 'placeholder-key',
    ```
    To:
    ```typescript
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    ```
    This matches the pattern already used in lib/supabase/client.ts.

    **INFRA-02 — RLS policy tightening migration:**
    Create `supabase/migrations/003_rls_tighten.sql` with the following policies.
    IMPORTANT: Present the final policy set only. No draft-then-correction narrative.

    ```sql
    -- ============================================
    -- Migration 003: Tighten RLS policies by role
    -- ============================================

    -- Drop overly broad "manage" policies
    DROP POLICY IF EXISTS "Authenticated users can manage menu plans" ON menu_plans;
    DROP POLICY IF EXISTS "Authenticated users can manage menu plan items" ON menu_plan_items;
    DROP POLICY IF EXISTS "Authenticated users can manage shopping list items" ON shopping_list_items;
    DROP POLICY IF EXISTS "Authenticated users can manage shopping lists" ON shopping_lists;

    -- Menu plans: only client role can write
    CREATE POLICY "Client can insert menu plans"
      ON menu_plans FOR INSERT
      TO authenticated
      WITH CHECK (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'client')
      );

    CREATE POLICY "Client can update menu plans"
      ON menu_plans FOR UPDATE
      TO authenticated
      USING (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'client')
      );

    CREATE POLICY "Client can delete menu plans"
      ON menu_plans FOR DELETE
      TO authenticated
      USING (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'client')
      );

    -- Menu plan items: only client role can write
    CREATE POLICY "Client can insert menu plan items"
      ON menu_plan_items FOR INSERT
      TO authenticated
      WITH CHECK (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'client')
      );

    CREATE POLICY "Client can update menu plan items"
      ON menu_plan_items FOR UPDATE
      TO authenticated
      USING (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'client')
      );

    CREATE POLICY "Client can delete menu plan items"
      ON menu_plan_items FOR DELETE
      TO authenticated
      USING (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'client')
      );

    -- Shopping list items: INSERT open to both roles (client generates list on confirm, cook adds ad-hoc items per SHOP-07)
    CREATE POLICY "Authenticated can insert shopping list items"
      ON shopping_list_items FOR INSERT
      TO authenticated
      WITH CHECK (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND (role = 'client' OR role = 'cook'))
      );

    -- Shopping list items: UPDATE restricted to cook only (only cook checks off items)
    CREATE POLICY "Cook can update shopping list items"
      ON shopping_list_items FOR UPDATE
      TO authenticated
      USING (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'cook')
      );

    -- Shopping lists: client creates (on confirm), both read (SELECT policies already exist)
    CREATE POLICY "Client can insert shopping lists"
      ON shopping_lists FOR INSERT
      TO authenticated
      WITH CHECK (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'client')
      );

    CREATE POLICY "Client can update shopping lists"
      ON shopping_lists FOR UPDATE
      TO authenticated
      USING (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'client')
      );

    CREATE POLICY "Client can delete shopping lists"
      ON shopping_lists FOR DELETE
      TO authenticated
      USING (
        EXISTS (SELECT 1 FROM profiles WHERE user_id = auth.uid() AND role = 'client')
      );

    -- Add unique constraint on menu_plans.visit_date (per D-04: one plan per date)
    ALTER TABLE menu_plans ADD CONSTRAINT menu_plans_visit_date_unique UNIQUE (visit_date);
    ```
  </action>
  <acceptance_criteria>
    - proxy.ts contains `pathname.startsWith('/menus')` in the cook redirect block
    - proxy.ts contains `pathname.startsWith('/lista')` in the client redirect block
    - lib/supabase/server.ts does NOT contain `placeholder.supabase.co`
    - lib/supabase/server.ts contains `process.env.NEXT_PUBLIC_SUPABASE_URL!`
    - supabase/migrations/003_rls_tighten.sql exists and contains `role = 'client'`
    - supabase/migrations/003_rls_tighten.sql contains `role = 'cook'`
    - supabase/migrations/003_rls_tighten.sql contains `menu_plans_visit_date_unique`
    - Shopping list items INSERT policy uses `(role = 'client' OR role = 'cook')` — both roles
    - Shopping list items UPDATE policy uses `role = 'cook'` — cook only
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/neilreeve-newson/Desktop/MiChef && npx tsc --noEmit</automated>
  </verify>
  <done>proxy.ts protects /menus and /lista by role; server.ts has no fallback; RLS migration created with role-scoped policies (INSERT open to both for shopping items, UPDATE cook-only) and visit_date unique constraint</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn components and update navigation</name>
  <files>components/ui/dialog.tsx, components/ui/checkbox.tsx, components/ui/separator.tsx, components/sidebar.tsx, components/bottom-nav.tsx</files>
  <read_first>
    - components/sidebar.tsx (navItems array structure)
    - components/bottom-nav.tsx (cookNavItems array structure)
    - components.json (shadcn config for install command)
  </read_first>
  <action>
    **Install shadcn components:**
    Run these commands:
    ```bash
    npx shadcn@latest add dialog
    npx shadcn@latest add checkbox
    npx shadcn@latest add separator
    ```

    **Update sidebar.tsx (per D-01 — no dedicated Menus sidebar section, keep sidebar lean):**
    In the `navItems` array, change the "Menu Planning" entry:
    - `href` from `'/menu-planning'` to `'/menus'`
    - Keep `active` as `false` — do NOT set it to true. Per D-01, menu planning is dashboard-first; the dashboard CTA (planned in 02-02) is the primary entry point, not the sidebar.

    Keep "Shopping Lists" as `active: false` — shopping lists are accessed via confirmed menu plans, not standalone.

    **Update bottom-nav.tsx:**
    Change the "Compras" nav item:
    - `href` from `'/compras'` to `'/lista'`
    - `labelEs` from `'Compras'` to `'Lista'`

    The icon remains `ShoppingCart` from lucide-react.
  </action>
  <acceptance_criteria>
    - components/ui/dialog.tsx exists
    - components/ui/checkbox.tsx exists
    - components/ui/separator.tsx exists
    - components/sidebar.tsx contains `href: '/menus'` with `active: false` in the menuPlanning nav item
    - components/sidebar.tsx does NOT contain `href: '/menu-planning'`
    - components/bottom-nav.tsx contains `href: '/lista'`
    - components/bottom-nav.tsx does NOT contain `href: '/compras'`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/neilreeve-newson/Desktop/MiChef && npx tsc --noEmit</automated>
  </verify>
  <done>shadcn dialog, checkbox, separator installed; sidebar href updated to /menus (kept active: false per D-01); bottom nav links to /lista</done>
</task>

<task type="auto">
  <name>Task 3: Add all Phase 2 i18n strings to both dictionaries</name>
  <files>lib/i18n/en.json, lib/i18n/es.json</files>
  <read_first>
    - lib/i18n/en.json (current structure and keys)
    - lib/i18n/es.json (current structure and keys)
    - .planning/phases/02-menu-planning-and-shopping/02-UI-SPEC.md (Copywriting Contract table — exhaustive list of all strings)
  </read_first>
  <action>
    Add the following keys to BOTH en.json and es.json. Place them as new top-level sections after the existing sections. All strings come from the UI-SPEC Copywriting Contract.

    **en.json additions:**
    ```json
    "menu": {
      "title": "Menu Plans",
      "planMenu": "Plan menu",
      "confirmMenu": "Confirm menu",
      "confirming": "Confirming...",
      "suggestMenu": "Suggest menu",
      "suggestLoading": "Finding the best recipes for this visit...",
      "suggestError": "Couldn't generate suggestions. Try again.",
      "confirmSelection": "Confirm selection",
      "selectionCount": "{{n}} of {{total}} selected",
      "removeRecipe": "Remove recipe",
      "deletePlan": "Delete plan",
      "deleteConfirm": "This will delete the plan for {{date}}. This cannot be undone.",
      "duplicateDate": "A plan for this date already exists. Edit the existing plan or choose a different date.",
      "noPlans": "No upcoming menus planned",
      "noPlansBody": "Plan your next visit to get started.",
      "visitDate": "Visit date",
      "recipes": "Recipes",
      "editPlan": "Edit plan",
      "draft": "Draft",
      "confirmed": "Confirmed",
      "saveError": "Couldn't save your changes. Try again.",
      "listGenError": "Couldn't create the shopping list. Confirm the menu again to retry.",
      "rulesWarning": "Cooking rules warning",
      "rulesWarningBody": "{{description}}. Review before confirming.",
      "whySuggested": "Why these recipes?",
      "addRecipes": "Add recipes",
      "searchRecipes": "Search recipes...",
      "recipesSelected": "{{n}} recipes selected"
    },
    "shopping": {
      "title": "Shopping List",
      "runningCount": "{{n}} of {{total}} checked",
      "emptyList": "Shopping list is empty",
      "emptyListBody": "No ingredients assigned yet.",
      "addItem": "Add item to shopping list",
      "addItemPlaceholder": "Add an item...",
      "fridgeStaples": "Always in stock",
      "checkError": "Failed to update item"
    },
    "cook": {
      "shoppingListLink": "Lista de compras",
      "noVisit": "No upcoming visit",
      "noVisitBody": "Your next cooking session will appear here when scheduled."
    }
    ```

    **es.json additions:**
    ```json
    "menu": {
      "title": "Planes de Menu",
      "planMenu": "Planificar menu",
      "confirmMenu": "Confirmar menu",
      "confirming": "Confirmando...",
      "suggestMenu": "Sugerir menu",
      "suggestLoading": "Buscando las mejores recetas para esta visita...",
      "suggestError": "No se pudieron generar sugerencias. Intentalo de nuevo.",
      "confirmSelection": "Confirmar seleccion",
      "selectionCount": "{{n}} de {{total}} seleccionados",
      "removeRecipe": "Quitar receta",
      "deletePlan": "Eliminar plan",
      "deleteConfirm": "Esto eliminara el plan para {{date}}. No se puede deshacer.",
      "duplicateDate": "Ya existe un plan para esta fecha. Edita el plan existente o elige otra fecha.",
      "noPlans": "No hay menus planificados",
      "noPlansBody": "Planifica tu proxima visita para comenzar.",
      "visitDate": "Fecha de visita",
      "recipes": "Recetas",
      "editPlan": "Editar plan",
      "draft": "Borrador",
      "confirmed": "Confirmado",
      "saveError": "No se pudieron guardar los cambios. Intentalo de nuevo.",
      "listGenError": "No se pudo crear la lista de compras. Confirma el menu de nuevo.",
      "rulesWarning": "Advertencia de reglas de cocina",
      "rulesWarningBody": "{{description}}. Revisa antes de confirmar.",
      "whySuggested": "Por que estas recetas?",
      "addRecipes": "Agregar recetas",
      "searchRecipes": "Buscar recetas...",
      "recipesSelected": "{{n}} recetas seleccionadas"
    },
    "shopping": {
      "title": "Lista de Compras",
      "runningCount": "{{n}} de {{total}} listos",
      "emptyList": "La lista esta vacia",
      "emptyListBody": "Aun no hay ingredientes asignados.",
      "addItem": "Agregar articulo a la lista",
      "addItemPlaceholder": "Agregar un articulo...",
      "fridgeStaples": "Siempre en casa",
      "checkError": "Error al actualizar el articulo"
    },
    "cook": {
      "shoppingListLink": "Lista de compras",
      "noVisit": "No hay visita proxima",
      "noVisitBody": "Tu proxima sesion aparecera aqui cuando sea programada."
    }
    ```
  </action>
  <acceptance_criteria>
    - lib/i18n/en.json contains `"planMenu": "Plan menu"`
    - lib/i18n/en.json contains `"runningCount"`
    - lib/i18n/en.json contains `"rulesWarning": "Cooking rules warning"`
    - lib/i18n/en.json contains `"addItem": "Add item to shopping list"`
    - lib/i18n/es.json contains `"planMenu": "Planificar menu"`
    - lib/i18n/es.json contains `"runningCount"`
    - lib/i18n/es.json contains `"emptyList": "La lista esta vacia"`
    - lib/i18n/es.json contains `"fridgeStaples": "Siempre en casa"`
    - Both files are valid JSON (node -e "JSON.parse(require('fs').readFileSync('lib/i18n/en.json'))" exits 0)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/neilreeve-newson/Desktop/MiChef && node -e "JSON.parse(require('fs').readFileSync('lib/i18n/en.json'))" && node -e "JSON.parse(require('fs').readFileSync('lib/i18n/es.json'))" && echo "Both JSON files valid"</automated>
  </verify>
  <done>All Phase 2 UI strings present in both en.json and es.json, matching the UI-SPEC Copywriting Contract</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- proxy.ts protects /menus (cook blocked) and /lista (client blocked)
- lib/supabase/server.ts has no placeholder fallback
- 003_rls_tighten.sql exists with role-scoped policies (INSERT both roles for shopping items, UPDATE cook-only)
- shadcn dialog, checkbox, separator installed in components/ui/
- Sidebar href updated to /menus (active: false per D-01), bottom nav links to /lista
- Both i18n JSON files valid and contain all Phase 2 keys
</verification>

<success_criteria>
Infrastructure tech debt resolved (INFRA-01, INFRA-02, INFRA-03). Navigation and i18n prepared for Phase 2 pages. All three infrastructure requirements verifiable: proxy route protection works for new routes, RLS is role-scoped, and env var fallback is removed. Sidebar Menu Planning kept inactive per D-01 (dashboard CTA is the primary entry point).
</success_criteria>

<output>
After completion, create `.planning/phases/02-menu-planning-and-shopping/02-01-SUMMARY.md`
</output>
