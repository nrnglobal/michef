---
phase: 02-menu-planning-and-shopping
plan: 04
type: execute
wave: 4
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements:
  - MENU-01
  - MENU-02
  - MENU-03
  - MENU-06
  - SHOP-02
  - SHOP-03
  - SHOP-04
  - SHOP-05
  - SHOP-06

must_haves:
  truths:
    - "Full happy-path flow works: create plan -> pick recipes -> confirm -> cook sees shopping list"
    - "Rules warnings appear when violations exist"
    - "Running count and checked item styling function correctly"
    - "Fridge staples section is visible and separate"
  artifacts: []
  key_links: []
---

<objective>
Full end-to-end verification of the menu planning and shopping list workflow. Human verifies both client and cook experiences.

Purpose: Catch visual, interaction, or data flow issues before marking the phase complete.
Output: Verified working flow or list of issues to fix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-menu-planning-and-shopping/02-CONTEXT.md
@.planning/phases/02-menu-planning-and-shopping/02-UI-SPEC.md
@.planning/phases/02-menu-planning-and-shopping/02-02-SUMMARY.md
@.planning/phases/02-menu-planning-and-shopping/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Full flow verification: menu planning through shopping list</name>
  <files>app/(client)/menus/page.tsx, app/(client)/menus/[id]/page.tsx, app/(cook)/lista/page.tsx, app/(cook)/visita/page.tsx</files>
  <read_first>
    - .planning/phases/02-menu-planning-and-shopping/02-UI-SPEC.md (visual reference)
  </read_first>
  <action>
    Start the dev server with `npm run dev`. Present the following verification checklist to the user and wait for their approval or issue reports.

    **What was built:**
    - Infrastructure fixes (proxy routes, RLS, env fallback)
    - Menu plan CRUD (create, edit, confirm, delete)
    - Recipe picker modal with search and filter
    - Rules engine with real-time warnings
    - AI suggest menu (requires CLAUDE_API_KEY in .env.local)
    - Auto-generated shopping list on confirm
    - Cook shopping list with check-off, running count, fridge staples, ad-hoc items
    - Dashboard Plan menu CTA
    - Updated sidebar and cook bottom nav

    **Client flow (login as client user):**
    1. Visit /dashboard — verify "Plan menu" CTA card is visible
    2. Click "Plan menu" or navigate to /menus — verify empty state shows if no plans exist
    3. Click "Plan menu" button on /menus page — verify /menus/new loads with date input
    4. Select a future date and submit — verify redirect to /menus/[id] edit page
    5. Click "Add recipes" — verify recipe picker modal opens with search bar, category tabs, recipe cards
    6. Search for a recipe — verify results filter correctly
    7. Select 3 recipes (verify checkmark overlay appears, counter shows "3 of 4 selected")
    8. Click "Confirm selection" — verify modal closes, recipe slots populated
    9. If CLAUDE_API_KEY is set: click "Suggest menu" — verify loading skeleton appears, then suggestions replace slots with rationale
    10. Verify rules warnings panel appears if applicable
    11. Click "Confirm menu" — verify spinner shows, then redirect to /menus with status badge showing "Confirmed"
    12. Check sidebar — verify "Menu Planning" link is active and points to /menus

    **Cook flow (login as cook user or open incognito):**
    13. Visit /visita — verify confirmed menu recipes show in Spanish (title_es)
    14. Verify "Lista de compras" link button appears below recipes
    15. Click the shopping list link — navigate to /lista
    16. Verify shopping list items display in Spanish with quantity and unit
    17. Verify items are grouped by category with section headers
    18. Verify fridge staples section appears at bottom with "Siempre en casa" label
    19. Tap a checkbox — verify it checks immediately (optimistic), running count updates
    20. Verify checked item shows opacity-50 and muted text
    21. Tap floating "+" button — verify input appears
    22. Type an item name and press Enter — verify item appears in the list
    23. Verify bottom nav shows "Lista" tab pointing to /lista

    **Edge cases:**
    24. Try creating a plan for a date that already has one — verify duplicate date warning
    25. Try confirming with fewer than 2 recipes — verify button is disabled or warning shown
    26. Uncheck a checked item — verify it reverts to unchecked state
  </action>
  <verify>
    <automated>cd /Users/neilreeve-newson/Desktop/MiChef && npx tsc --noEmit && echo "Type check passes"</automated>
  </verify>
  <done>Human approves all 26 verification steps. No console errors. All Spanish text renders correctly. Touch targets are 48px+. Running count format is "N de M listos".</done>
</task>

</tasks>

<verification>
Human verifies full end-to-end flow works as specified.
</verification>

<success_criteria>
Human approves the complete Phase 2 workflow after testing all critical paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-menu-planning-and-shopping/02-04-SUMMARY.md`
</output>
