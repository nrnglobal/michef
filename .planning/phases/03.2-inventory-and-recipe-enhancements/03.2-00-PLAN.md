---
phase: 03.2-inventory-and-recipe-enhancements
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/components/inventory-client.test.ts
  - tests/lib/autocomplete.test.ts
  - tests/lib/sheets-import.test.ts
  - tests/components/recipe-detail-actions.test.ts
autonomous: true
requirements: [UX-05, UX-06, UX-08, UX-10]

must_haves:
  truths:
    - "Wave 0 test stubs exist for all phase requirements before implementation begins"
    - "npx vitest run passes with all stubs green (placeholder assertions)"
  artifacts:
    - path: "tests/components/inventory-client.test.ts"
      provides: "Test stubs for UX-10 (ItemForm module scope) and UX-05 (autocomplete)"
    - path: "tests/lib/autocomplete.test.ts"
      provides: "Test stubs for UX-05 (grocery item filtering)"
    - path: "tests/lib/sheets-import.test.ts"
      provides: "Test stubs for UX-06 (sheet ID extraction, CSV parsing)"
    - path: "tests/components/recipe-detail-actions.test.ts"
      provides: "Test stubs for UX-08 (menu query includes confirmed menus)"
  key_links: []
---

<objective>
Create Wave 0 test stubs for all phase 03.2 requirements so that subsequent plans have automated verification targets via `npx vitest run`.

Purpose: The Nyquist rule requires every task's `<verify>` to include an automated test command. These stubs establish the test files that Wave 1 and Wave 2 plans will fill with real assertions as implementation progresses.

Output: Four test files with placeholder describe/it blocks covering UX-05, UX-06, UX-08, and UX-10.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-inventory-and-recipe-enhancements/03.2-CONTEXT.md
@.planning/phases/03.2-inventory-and-recipe-enhancements/03.2-RESEARCH.md

<interfaces>
<!-- Existing test pattern from tests/lib/image-resize.test.ts -->
```typescript
import { describe, it, expect } from 'vitest'

describe('resizeImageToLimit', () => {
  it('should be defined as a function', () => {
    expect(true).toBe(true)
  })
})
```

vitest.config.ts confirms:
- environment: jsdom
- include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx']
- globals: true
- alias: '@' -> project root
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for inventory and autocomplete (UX-05, UX-10)</name>
  <files>
    tests/components/inventory-client.test.ts,
    tests/lib/autocomplete.test.ts
  </files>
  <action>
1. Create `tests/components/inventory-client.test.ts` with placeholder stubs:

```typescript
import { describe, it, expect } from 'vitest'

describe('InventoryClient — ItemForm module scope (UX-10)', () => {
  it('ItemForm is defined at module scope, not inside InventoryClient', () => {
    // Wave 1 will read inventory-client.tsx source and verify ItemForm
    // is declared before InventoryClient export
    expect(true).toBe(true)
  })
})

describe('InventoryClient — Autocomplete (UX-05)', () => {
  it('typing in English name field filters grocery items', () => {
    // Wave 1 will test that filtering groceryItems by partial string works
    expect(true).toBe(true)
  })

  it('selecting a suggestion fills Spanish name and category', () => {
    // Wave 1 will test the onSuggestionSelect callback behavior
    expect(true).toBe(true)
  })

  it('custom items not in database are accepted', () => {
    // Wave 1 will verify form submits with non-database item names
    expect(true).toBe(true)
  })
})
```

2. Create `tests/lib/autocomplete.test.ts` with placeholder stubs:

```typescript
import { describe, it, expect } from 'vitest'

describe('Grocery autocomplete filtering (UX-05)', () => {
  it('filters grocery items by English name (case-insensitive)', () => {
    // Wave 1 will import grocery-items.json and test filtering
    expect(true).toBe(true)
  })

  it('returns empty array for no matches', () => {
    expect(true).toBe(true)
  })

  it('limits results to max 8 suggestions', () => {
    expect(true).toBe(true)
  })

  it('matches partial strings (substring, not prefix-only)', () => {
    expect(true).toBe(true)
  })
})
```
  </action>
  <verify>
    <automated>cd /Users/neilreeve-newson/Desktop/MiChef && test -f tests/components/inventory-client.test.ts && test -f tests/lib/autocomplete.test.ts && npx vitest run tests/components/inventory-client.test.ts tests/lib/autocomplete.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist and pass with placeholder assertions. Ready for Wave 1 plans to fill with real tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for sheets import and menu expansion (UX-06, UX-08)</name>
  <files>
    tests/lib/sheets-import.test.ts,
    tests/components/recipe-detail-actions.test.ts
  </files>
  <action>
1. Create `tests/lib/sheets-import.test.ts` with placeholder stubs:

```typescript
import { describe, it, expect } from 'vitest'

describe('Google Sheets import — URL parsing (UX-06)', () => {
  it('extracts sheet ID from standard edit URL', () => {
    // Wave 2 will test: https://docs.google.com/spreadsheets/d/ABC123/edit#gid=0
    expect(true).toBe(true)
  })

  it('extracts sheet ID from sharing URL', () => {
    // Wave 2 will test: https://docs.google.com/spreadsheets/d/ABC123/edit?usp=sharing
    expect(true).toBe(true)
  })

  it('returns null for non-Google-Sheets URLs', () => {
    expect(true).toBe(true)
  })
})

describe('Google Sheets import — CSV parsing (UX-06)', () => {
  it('parses CSV with correct headers into objects', () => {
    // Wave 2 will test manual CSV parsing by header name
    expect(true).toBe(true)
  })

  it('skips rows with blank item_name_en', () => {
    expect(true).toBe(true)
  })

  it('defaults category to Other for invalid values', () => {
    expect(true).toBe(true)
  })
})
```

2. Create `tests/components/recipe-detail-actions.test.ts` with placeholder stubs:

```typescript
import { describe, it, expect } from 'vitest'

describe('RecipeDetailActions — Add to Menu (UX-08)', () => {
  it('menu query does not filter by draft status', () => {
    // Wave 1 will read recipe-detail-actions.tsx source and verify
    // no .eq('status', 'draft') in the query
    expect(true).toBe(true)
  })

  it('menu items include status field', () => {
    // Wave 1 will verify status is in the select and state type
    expect(true).toBe(true)
  })

  it('renders Confirmed and Draft badges', () => {
    // Wave 1 will verify badge text appears in JSX
    expect(true).toBe(true)
  })
})
```
  </action>
  <verify>
    <automated>cd /Users/neilreeve-newson/Desktop/MiChef && test -f tests/lib/sheets-import.test.ts && test -f tests/components/recipe-detail-actions.test.ts && npx vitest run tests/lib/sheets-import.test.ts tests/components/recipe-detail-actions.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist and pass with placeholder assertions. Ready for Wave 1 (recipe-detail-actions) and Wave 2 (sheets-import) plans to fill with real tests.</done>
</task>

</tasks>

<verification>
1. `npx vitest run --reporter=verbose` — all existing tests plus 4 new stub files pass
2. All 4 test files exist: `tests/components/inventory-client.test.ts`, `tests/lib/autocomplete.test.ts`, `tests/lib/sheets-import.test.ts`, `tests/components/recipe-detail-actions.test.ts`
</verification>

<success_criteria>
- Four test stub files created matching RESEARCH.md Wave 0 gaps
- All stubs pass with `npx vitest run`
- No existing tests broken
- Wave 1 and Wave 2 plans can now reference these files in their verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-inventory-and-recipe-enhancements/03.2-00-SUMMARY.md`
</output>
