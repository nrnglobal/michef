---
phase: 03.2-inventory-and-recipe-enhancements
plan: 03
subsystem: api, ui
tags: [google-sheets, csv, import, inventory, claude-haiku, translation, supabase]

# Dependency graph
requires:
  - phase: 03.2-inventory-and-recipe-enhancements
    provides: "FridgeStaple type with source/brand fields, ItemForm at module scope, autocomplete"
provides:
  - "POST /api/inventory/import-sheets route for server-side Google Sheets CSV fetch"
  - "Batch translation of blank Spanish names via Claude Haiku"
  - "Collapsible import panel with duplicate resolution in inventory UI"
  - "Downloadable CSV template at /templates/inventory-template.csv"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Server-side proxy for external CSV fetch to avoid CORS"
    - "Single batch Claude call for bulk translation (not one per item)"
    - "Inline duplicate resolution with per-item skip/update decisions"

key-files:
  created:
    - app/api/inventory/import-sheets/route.ts
    - public/templates/inventory-template.csv
  modified:
    - app/(client)/inventory/inventory-client.tsx

key-decisions:
  - "Manual CSV parsing (split by comma) instead of papaparse -- template is app-controlled so edge cases are predictable"
  - "Single batch Claude Haiku call for all blank Spanish names rather than per-item translate endpoint calls"

patterns-established:
  - "Server-side Google Sheets fetch pattern: extract sheet ID from URL, fetch export?format=csv"
  - "Inline duplicate resolution: per-item skip/update buttons in import results panel"

requirements-completed: [UX-06]

# Metrics
duration: 3min
completed: 2026-03-26
---

# Phase 03.2 Plan 03: Google Sheets Import Summary

**Server-side Google Sheets CSV import with batch Claude translation and inline duplicate resolution for inventory bulk population**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-26T21:58:29Z
- **Completed:** 2026-03-26T22:01:48Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Server-side API route fetches Google Sheets CSV to avoid CORS, parses by header name
- Blank Spanish names batch-translated in a single Claude Haiku call
- Collapsible import panel with URL input, duplicate detection, per-item skip/update resolution
- Downloadable CSV template with correct 7-column format (no cost column per D-21)

## Task Commits

Each task was committed atomically:

1. **Task 1: Create import-sheets API route and CSV template** - `c09b6f8` (feat)
2. **Task 2: Add Google Sheets import UI to inventory page** - `f90930c` (feat)

## Files Created/Modified
- `app/api/inventory/import-sheets/route.ts` - POST handler: extracts sheet ID, fetches CSV server-side, parses by header, batch translates blank Spanish names, validates categories
- `public/templates/inventory-template.csv` - Downloadable template with item_name_en, item_name_es, quantity, category, source, brand, is_staple columns and 2 example rows
- `app/(client)/inventory/inventory-client.tsx` - Added ImportRow type, import state variables, handleFetchSheet/handleConfirmImport handlers, collapsible import panel with duplicate resolution UI

## Decisions Made
- Manual CSV parsing (split by comma) instead of papaparse -- template is app-controlled so edge cases are predictable
- Single batch Claude Haiku call for all blank Spanish names rather than per-item translate endpoint calls (avoids Pitfall 6 from RESEARCH.md)

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None - all data flows are wired end-to-end.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required. Uses existing ANTHROPIC_API_KEY.

## Next Phase Readiness
- Google Sheets import feature complete (UX-06)
- Inventory page now supports both manual entry and bulk import
- Ready for remaining Phase 03.2 plans

## Self-Check: PASSED

All 3 created/modified files verified on disk. Both task commits (c09b6f8, f90930c) verified in git log.

---
*Phase: 03.2-inventory-and-recipe-enhancements*
*Completed: 2026-03-26*
