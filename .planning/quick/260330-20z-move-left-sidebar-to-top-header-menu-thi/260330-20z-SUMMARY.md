---
phase: quick
plan: 260330-20z
subsystem: navigation
tags: [nav, header, sidebar, layout, ux]
dependency_graph:
  requires: []
  provides: [top-header-navigation]
  affects: [app/(client)/layout.tsx, components/header-nav.tsx]
tech_stack:
  added: []
  patterns: [sticky-header, horizontal-nav, responsive-icon-labels]
key_files:
  created:
    - components/header-nav.tsx
  modified:
    - app/(client)/layout.tsx
decisions:
  - "sidebar.tsx kept on disk (not deleted) — plan explicitly deferred cleanup to a separate task"
  - "Nav link active matching uses pathname.startsWith(href + '/') to avoid false positives on prefix routes"
metrics:
  duration: "~5 min"
  completed: "2026-03-30"
  tasks: 1
  files: 2
---

# Quick 260330-20z: Move Left Sidebar to Top Header Navigation

**One-liner:** Replaced the 240px left sidebar with a sticky horizontal header bar containing logo, nav links (icons + labels), user name, ThemeToggle, and logout — using the same casa-* CSS variables as the sidebar.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Create HeaderNav component and update client layout | 60b3764 | components/header-nav.tsx, app/(client)/layout.tsx |

## What Was Built

- `components/header-nav.tsx` — `'use client'` component with sticky `h-14` header. Left: logo + "Mi Chef" text. Center: 5 nav links with icons and labels (labels hidden below `md` breakpoint). Right: user name (hidden below `md`), `ThemeToggle compact`, logout button.
- `app/(client)/layout.tsx` — Changed from `flex h-screen overflow-hidden` (side-by-side) to `min-h-screen flex flex-col`. Replaced `<Sidebar>` with `<HeaderNav>`.

## Deviations from Plan

None — plan executed exactly as written. `sidebar.tsx` intentionally left on disk per plan instruction.

## Known Stubs

None.
