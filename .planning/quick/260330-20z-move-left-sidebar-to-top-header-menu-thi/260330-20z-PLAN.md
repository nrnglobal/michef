---
phase: quick
plan: 260330-20z
type: execute
wave: 1
depends_on: []
files_modified:
  - components/header-nav.tsx
  - app/(client)/layout.tsx
  - components/sidebar.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Client sees a top header bar with logo, nav links, user info, theme toggle, and logout"
    - "No left sidebar is visible on any client page"
    - "Navigation links (Recipes, Menus, Shopping Lists, Inventory, Rules) work identically to the old sidebar"
    - "Active route is visually highlighted in the header"
    - "Layout works on both desktop and mobile widths"
  artifacts:
    - path: "components/header-nav.tsx"
      provides: "Top header navigation replacing sidebar"
    - path: "app/(client)/layout.tsx"
      provides: "Updated client layout using header instead of sidebar"
  key_links:
    - from: "app/(client)/layout.tsx"
      to: "components/header-nav.tsx"
      via: "import and render"
---

<objective>
Replace the left sidebar navigation in the client layout with a horizontal top header menu bar. The header should contain the same elements as the sidebar: logo/branding, navigation links, user name, theme toggle, and logout button -- arranged horizontally.

Purpose: Reclaim horizontal screen space for content and provide a more modern navigation pattern.
Output: New header-nav.tsx component, updated client layout, sidebar.tsx can be deleted.
</objective>

<context>
@components/sidebar.tsx
@app/(client)/layout.tsx
@components/theme-toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeaderNav component and update client layout</name>
  <files>components/header-nav.tsx, app/(client)/layout.tsx</files>
  <action>
Create `components/header-nav.tsx` as a 'use client' component that replaces the sidebar with a horizontal top header. It receives `userName` prop (same as Sidebar).

**Header structure (single horizontal bar, sticky top):**
- Left: ChefHat icon in casa-primary rounded box + "Mi Chef" text (same as sidebar logo section)
- Center/Left: Navigation links in a horizontal row -- Recipes, Menus, Shopping Lists, Inventory, Rules. Use the same `navItems` array, icons, hrefs, and i18n keys from sidebar.tsx. Active route gets casa-primary background + white text (same pattern as sidebar active state).
- Right: User name display (text-sm, truncated), ThemeToggle (compact mode), Logout button (LogOut icon only on desktop to save space, or icon+text).

**Styling:**
- Use `sticky top-0 z-40` so it stays visible on scroll
- Background: `var(--casa-surface)` with `border-b` using `var(--casa-border)` -- matches sidebar
- Height: ~56-64px (h-14 or h-16)
- Horizontal padding: px-4 or px-6
- Use flexbox with `items-center justify-between`
- Nav links: `flex items-center gap-1` with each link having icon + label, rounded-lg padding, text-sm font-medium
- Active state: same `var(--casa-primary)` bg + `var(--casa-surface)` text as sidebar
- Inactive state: `var(--casa-text-dark)` color with hover:bg-gray-50

**Mobile responsive:**
- On small screens (below md), hide nav link labels and show only icons
- Or use a hamburger menu -- simplest approach: hide labels below md breakpoint (`hidden md:inline`)
- User name hidden below md, just show theme toggle + logout icon

**Logout handler:** Same `createClient().auth.signOut()` then `router.push('/login')` pattern from sidebar.

**Update `app/(client)/layout.tsx`:**
- Replace `import { Sidebar }` with `import { HeaderNav }` from `@/components/header-nav`
- Change layout from `flex h-screen overflow-hidden` (side-by-side) to a vertical stack:
  ```
  <div className="min-h-screen flex flex-col" style={{ backgroundColor: 'var(--casa-bg)' }}>
    <HeaderNav userName={userName} />
    <main className="flex-1 overflow-y-auto">
      <div className="max-w-6xl mx-auto px-6 py-8">{children}</div>
    </main>
  </div>
  ```
- Remove the Sidebar import entirely

**Do NOT delete sidebar.tsx yet** -- just remove its import from the client layout. It can be cleaned up separately.

**Important:** Use the same `var(--casa-*)` CSS custom properties as sidebar.tsx for all colors. Do NOT use hardcoded Tailwind color classes for brand colors. Keep the chef-abuela image out of the header (it was a sidebar decoration and does not fit a header bar).
  </action>
  <verify>
    <automated>cd /Users/neilreeve-newson/Desktop/MiChef && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Header nav renders at top of all client pages with working navigation links, active state highlighting, user name, theme toggle, and logout. No sidebar visible. Build succeeds with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Replaced left sidebar with a horizontal top header navigation bar on all client pages. Navigation links, active state highlighting, user info, theme toggle, and logout are all in the header.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000/recipes (or any client page)
    2. Confirm: No left sidebar is visible
    3. Confirm: Top header bar shows logo, nav links (Recipes, Menus, Shopping Lists, Inventory, Rules), user name, theme toggle, logout
    4. Click each nav link -- confirm navigation works and active link is highlighted
    5. Resize browser to mobile width -- confirm header is still usable (icons may replace labels)
    6. Click theme toggle -- confirm dark/light switch works
    7. Click logout -- confirm redirect to /login
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- No imports of `Sidebar` remain in `app/(client)/layout.tsx`
- `components/header-nav.tsx` exists and exports `HeaderNav`
- All nav links from sidebar are present in header
</verification>

<success_criteria>
- Left sidebar is fully replaced by a top header menu on all client routes
- All navigation, user info, theme toggle, and logout functionality preserved
- Layout is responsive and usable on mobile
- Build passes cleanly
</success_criteria>

<output>
After completion, update `.planning/STATE.md` quick tasks table.
</output>
